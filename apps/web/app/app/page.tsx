import { ComposerWorkbench } from "../../components/composer-workbench";

export const metadata = {
  title: "Workbench | Socioconnect",
  description: "Compose once, dispatch independently to every social destination.",
};

export default function WorkbenchPage() {
  return <ComposerWorkbench />;
}
